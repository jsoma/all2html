# Canva-Oriented SVG Fixtures

These fixtures model the kinds of SVG output the importer is expected to handle well for the Canva workflow:

- responsive filename grouping
- mixed live text and vector backgrounds
- embedded raster images
- transformed text fallback
- masks, clips, and filters preserved in raster output

They are committed fixtures for importer hardening, not runtime dependencies.
